import { base } from '@theme-ui/presets';

export const theme = {
  ...base,
  buttons: {
    primary: {
      color: 'background',
      bg: 'primary',
    },
    secondary: {
      color: 'background',
      bg: 'secondary',
    },
  },
};
