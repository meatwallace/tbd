import { RiCheckboxBlankCircleLine } from 'react-icons/ri';

type Props = {
  size: number;
};

export function CircleCheckboxBlank(props: Props) {
  return <RiCheckboxBlankCircleLine {...props} />;
}
